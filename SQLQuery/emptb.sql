show databases;

use group024;

create table emptb(ID int not null auto_increment,Name varchar(50),Password varchar(20),Email varchar(50),Gender varchar(20),Country varchar(50), primary key(id));


desc emptb;

select * from emptb;